echo "🧪 Running pre-push checks..."
echo "-----------------------------"
echo

yarn build
if [ $? -eq 0 ]; then
  echo
  echo "✅ Build succeeded. Pushing your changes."
  echo
else
  echo
  echo "❌ Build failed. Fix errors before pushing."
  echo
  exit 1
fi
